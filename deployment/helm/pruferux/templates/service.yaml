apiVersion: v1
kind: Service
metadata:
  name: {{ include "pruferux.fullname" . }}
  labels:
    {{- include "pruferux.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pruferux.selectorLabels" . | nindent 4 }}
