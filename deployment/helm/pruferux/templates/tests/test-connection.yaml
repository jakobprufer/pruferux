apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pruferux.fullname" . }}-test-connection"
  labels:
    {{- include "pruferux.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pruferux.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
