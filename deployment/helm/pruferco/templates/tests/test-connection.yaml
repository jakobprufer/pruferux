apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pruferco.fullname" . }}-test-connection"
  labels:
    {{- include "pruferco.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pruferco.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
