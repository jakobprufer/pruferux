# Default values for pruferco.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

global:
  appVersion: ""

replicaCount: 1

image:
  repository: cr.romnn.com/pruferco/frontend
  pullPolicy: IfNotPresent
  # imagePullSecrets:
  #   - name: pruferco-container-pull-secret

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext:
  {}
  # fsGroup: 2000

securityContext:
  {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 80


ingress:
  # - name: non-ux
  #   enabled: true
  #   annotations:
  #     kubernetes.io/ingress.class: nginx
  #     kubernetes.io/tls-acme: "true"
  #     cert-manager.io/cluster-issuer: letsencrypt-staging
  #     nginx.ingress.kubernetes.io/upstream-vhost: prufer.co
  #   hosts:
  #     - host: ux.prufer.co
  #       paths:
  #         - path: /(.*)
  #           pathType: ImplementationSpecific
  #         - path: /(home|mould|senses|home|hair|birdsong|disguise)
  #           pathType: Prefix
  #   tls:
  #     - secretName: prufer.co
  #       hosts:
  #         - ux.prufer.co

  # - name: ux
  #   enabled: true
  #   annotations:
  #     kubernetes.io/ingress.class: nginx
  #     kubernetes.io/tls-acme: "true"
  #     cert-manager.io/cluster-issuer: letsencrypt-staging
  #     nginx.ingress.kubernetes.io/rewrite-target: /ux/$1
  #     # nginx.ingress.kubernetes.io/upstream-vhost: prufer.co
  #   hosts:
  #     - host: ux.prufer.co
  #       paths:
  #         # - path: /(.*)
  #         #   pathType: Prefix
  #         # - path: /
  #         - path: /(home|mould|senses|hair|birdsong|disguise)
  #           # pathType: ImplementationSpecific
  #           pathType: Prefix
  #   tls:
  #     - secretName: prufer.co
  #       hosts:
  #         - ux.prufer.co

  - name: root
    enabled: true
    annotations:
      kubernetes.io/ingress.class: nginx
      kubernetes.io/tls-acme: "true"
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hosts:
      # - host: ux.prufer.co
      #   paths:
      #     # - path: /(home|mould|senses|home|hair|birdsong|disguise)
      #     - path: /(.*)
      #       pathType: Prefix
      #     # - path: /
      #     #   pathType: Prefix

      - host: prufer.co
        paths:
          - path: /
            pathType: ImplementationSpecific
      # - host: ux.prufer.co
      #   paths:
      #     - path: /
      #       pathType: ImplementationSpecific

      # - host: ux.prufer.co
      #   paths:
      #     - path: /static
      #       # pathType: ImplementationSpecific
      #       pathType: Prefix
      #     - path: /images
      #       # pathType: ImplementationSpecific
      #       pathType: Prefix
      # - ux.prufer.co
    tls:
      - secretName: prufer.co
        hosts:
          - prufer.co

resources:
  limits:
    # cpu: 100m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 32Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 75
  targetMemoryUtilizationPercentage: 75

nodeSelector: {}

tolerations: []

affinity: {}

# registry:
#   name: https://core.registry.romnn.com
#   user: changeme
#   password: changeme
